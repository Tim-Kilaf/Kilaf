const { Users, Sequelize } = require('../models')

async function resolveBids() {
    console.log('resolve bids');
    // const users = await Users.findAll()
    // console.log(users);
}

module.exports = resolveBids